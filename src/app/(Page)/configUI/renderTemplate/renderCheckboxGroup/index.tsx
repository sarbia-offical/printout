import { IRenderConfigProps } from "@/app/interface";
import { useEffect } from "react";

const RenderCheckboxGroup = (props: IRenderConfigProps) => {
    return (
        <div>
        </div>
    )
}
export default RenderCheckboxGroup;
import { IRenderConfigProps } from "@/app/interface";
import { useEffect } from "react";

const RenderSpaceTag = (props: IRenderConfigProps) => {
    return (
        <div>
        </div>
    )
}
export default RenderSpaceTag;